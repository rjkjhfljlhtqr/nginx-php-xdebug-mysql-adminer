{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for Xdebug",
            "type": "php",
            "request": "launch",
            "hostname": "localhost",
            "port": 9003,
            "pathMappings": {
                "/var/www/html": "${workspaceFolder}/web/public"
            }
        }
    ]
}
